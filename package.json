{
  "name": "patternstore",
  "displayName": "PatternStore",
  "description": "Save and recall reusable regex find/replace pairs in VS Code",
  "version": "0.1.0",
  "publisher": "JoachimDunkel",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "patternStore.save",
        "title": "PatternStore: Save Pattern"
      },
      {
        "command": "patternStore.load",
        "title": "PatternStore: Load Pattern"
      },
      {
        "command": "patternStore.manage",
        "title": "PatternStore: Manage Patterns"
      }
    ],
    "configuration": {
      "title": "PatternStore",
      "properties": {
        "patternStore.savedPatterns": {
          "type": "array",
          "default": [],
          "description": "Global patterns available in all workspaces",
          "items": {
            "type": "object",
            "properties": {
              "label": {
                "type": "string"
              },
              "find": {
                "type": "string"
              },
              "replace": {
                "type": "string"
              },
              "flags": {
                "type": "object"
              },
              "scope": {
                "type": "string"
              }
            }
          }
        },
        "patternStore.workspacePatterns": {
          "type": "array",
          "default": [],
          "description": "Workspace-specific patterns",
          "scope": "resource",
          "items": {
            "type": "object",
            "properties": {
              "label": {
                "type": "string"
              },
              "find": {
                "type": "string"
              },
              "replace": {
                "type": "string"
              },
              "flags": {
                "type": "object"
              },
              "scope": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "keybindings": [
      {
        "command": "patternStore.load",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.x",
    "@typescript-eslint/parser": "^6.x",
    "eslint": "^8.x",
    "typescript": "^5.3.0"
  }
}
